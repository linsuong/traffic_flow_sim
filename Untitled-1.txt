code outline:
strategy of inputing the car
randomly assign each car a cell in the latice with some speed
keep safe distance - maybe 1 car length (with some conditions - more speed more distance)
check everytime for safe distance
kindness = space left between car

fix time 


    def initialize(self):
        #print(self.Road.length)
        self.Vehicle.position = random.sample(range(self.Road.length), int(self.Road.length * self.Road.density)) 
        self.Vehicle.position.sort() 
        
        #print(positions)
        print(self.Vehicle.position)
        self.Vehicle.velocity = [random.randint(0, self.Road.speed_limit) for i in range(len(self.Vehicle.position))]
        
        print(velocities)


            #print(self.data[i][1])
            print('Vehicle ID: %s' %i)
            new_data = [item[i] for item in self.data]
            print('Position List: %s' %new_data)
            plt.plot(new_data, time_steps, '.')
